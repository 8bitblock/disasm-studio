# DisasmStudio Midnight UI concept v2 edit prompt

Edited from `disasmstudio-ui-midnight-concept.png` with the built-in ImageGen tool on 2026-08-30.

```text
Use case: precise-object-edit
Asset type: revised high-fidelity desktop UI mockup
Input images: Image 1 is the edit target.

Primary request: Edit Image 1 with exactly these four UX refinements, using the annotated coordinate intent:
1. Around x 17.7%, y 18.9%: make the entire top-level workbench tab strip (“Projects” through “Prism”) substantially smaller and denser. Reduce its vertical height and internal padding by roughly one third, use compact single-line labels, preserve every tab label and keep “Binary View” active. Use the reclaimed space for the main work area.
2. Around x 36.9%, y 85.8%: increase information density in list/table panes, especially the lower “Breakpoints” table. Use tighter row heights, smaller vertical cell padding, and compact readable typography so more items fit. Show five credible breakpoint rows instead of two while keeping headers “Address”, “Condition”, “Hits” crisp and readable. Apply the same compact row-density language to the right-side Functions list.
3. Around x 80.5%, y 75.1%: make the entire lower inspector pane collapsible. Add one clear, restrained square collapse button with a downward chevron at the far-right end of the lower pane’s tab header. The pane remains expanded in this image; the control visibly communicates that it can collapse down to the tab bar.
4. Around x 38%, y 25.5%: show “Live Assembly” as unavailable during static analysis. Keep the label in its existing position but render it clearly disabled in low-contrast muted gray at about 35% opacity, with no blue underline or active styling.

Style: retain the exact Midnight graphite/navy DisasmStudio design, cyan-blue active states, green coverage markers, amber guessed badge, red breakpoints, typography, one-pixel separators, and realistic Windows desktop appearance.

Constraints and invariants: change only the four requested areas. Preserve the original 16:9 framing, application title, menus, document tabs, debugger toolbar, all other top-level labels, assembly code content, selected assembly row, breakpoint marker, right-side panel structure, bottom tab labels, status bar, proportions, and overall visual identity. Keep all existing visible text correctly spelled. Do not redesign, recolor, crop, blur, introduce new panels, add annotations/arrows/callout labels, add a watermark, or turn the image into a wireframe.
```
