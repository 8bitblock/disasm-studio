# Workflow workbench visual concept

Generated with the built-in imagegen tool. This is an illustrative layout study, not a screenshot or analysis result. The implementation must use actual evidence states and never infer percentages from this image.

```text
Use case: ui-mockup
Asset type: DisasmStudio native desktop workbench concept, 16:10 landscape high fidelity flat screenshot.
Primary request: Refine a professional reverse engineering and crackme analysis application around analyst intent. Keep one fixed window, no floating panels, no ornamental artwork. Make dense assembly readable and navigation calm and immediate.
Composition: slim menu and document tab header; a clear compact workflow preset row labeled Analyze, Debug, Memory, Compare with Analyze selected and Ctrl+K command search on the right. Persistent breadcrumb below: crackme.exe / FILE / .text / validate_input / 0x140001280. Left narrow function navigator, center wide Assembly / Pseudocode / Hex / Graph tabs with legible monospaced assembly lines and a selected conditional branch, right evidence inspector showing activation stages and confidence: Input observed, Format candidate, Server acceptance unknown, Signature unknown, Feature gate observed. Short lower drawer with Types, References, Trace tabs; Types shows LicenseState with fields at +0x00, +0x04 and +0x08. Bottom quiet analysis queue showing Selected function prioritized.
Style: meticulous native productivity UI, neutral graphite surfaces, restrained cool blue selected accents, subtle one-pixel dividers, generous code line rhythm, compact supporting UI, clear light-gray text, amber uncertainty and green observed evidence. No gradients, glows, large cards, fake chart decoration, marketing copy or exterior device frame. All controls should look practical to implement in Dear ImGui.
```
