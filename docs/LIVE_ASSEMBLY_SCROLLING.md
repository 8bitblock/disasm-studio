# Live Assembly scrolling

- Fixed the upward scrolling wall after following a LIVE string reference near an analyzed function start. The earlier-window request excluded the current function anchor once, but the next idle frame selected that anchor again and discarded the earlier instructions.
- Retain the committed live window's start through idle frames, manual browsing, register changes and content refreshes. Alignment reads and trial decoding now occur when choosing a new window, instead of on every rendered frame.
- Preserve an overlapping instruction's vertical position when the bounded buffer refills. Forward browsing starts from a decoded midpoint and retains half the old rows; backward browsing examines up to 256 readable bytes and retains at most 128 preceding rows. Reads remain bounded to 4 KiB and the displayed cache to 256 instructions, with no total browsing-distance cap within contiguous readable memory.
- Backward decoding preserves the committed first instruction as a separate decode boundary. A run of x86 prefix bytes immediately before a selected NOP can make every trial decode consume that NOP, which previously created an absolute upward wall despite readable memory. When trial alignment cannot reach the committed boundary, the earlier extent still appears; incomplete preceding bytes use the decoder's existing `db` representation. Prefix and suffix decoding are independently bounded to 256 instructions before the retained cache is trimmed.
- Focus and refill positioning allow up to three corrections for ImGui's deferred scrolling and frozen-header/cell layout, then retire. New manual input cancels remaining pan corrections. Verification measures actual address glyph positions after settling, rather than assuming every table row has the last row's height.
- Keep the previous extent visible when an edge refill cannot advance or read memory. A later input can retry; a failed candidate cannot silently replace the window on the next frame.
- Read the live decode buffer in page-bounded pieces and verify the debugger session before adoption, so an unreadable next page does not hide valid instructions preceding it.
- Release Follow RIP on manual wheel, keyboard or scrollbar scrolling within the current window. Viewport movement preserves the selected instruction and navigation history; explicitly following the same reference again brings it back into view.
- Up/Down arrows explicitly scroll one row and request another window at the edge, while preserving selection/history. A scrollbar thumb held at either track end continues browsing after each refill. The held thumb controls viewport positioning; wheel refills retain the overlapping row position. Explicit pointer direction wins over temporary scroll geometry changes.
- Held-thumb detection uses the native child window's scrollbar ID. Looking up the ID inside the table's pushed scope produces a different ID and fails to recognize an actual drag; this was reproduced with the installed ImGui library.
- Reference handoffs retain their existing FILE/LIVE address spaces and debugger-session/module validation. This change does not alter execution or target memory.

## Verification

- The production-object `tests/live_scroll_fixture.inc` exercises the real Live Assembly renderer and checked debugger reads against a contained copy of the test harness. Its synthetic instructions and string occupy a test-owned allocation; it does not attach to an existing user process.
- The fixture covers uninterrupted wheel streams, both arrow-key edges, actual scrollbar thumb drags held at both ends, bounded cache size during distant browsing, and a 512-byte prefix run immediately before a committed NOP. Arrow setup waits for deferred cell layout to establish the actual edge before sending the key. Set `DS_LIVE_SCROLL_DIAGNOSTICS=1` for frame-level gesture diagnostics.
- Set `DS_LIVE_SCROLL_TEST=1` to include the live regression in `tests/run_static_listing_actions_test.ps1`, or pass `-LiveScrollOnly -OutputRoot build/live-scroll-focused` for a focused run with an incremental LTCG cache. Build Release x64 first so the harness links current production objects.
- Release x64 build and embedded GameMaker helper verification passed (`build/live-review-final-scroll-build.log`). The updated executable is `build/x64/Release/DisasmStudio.exe`.
- Focused live scrolling passed with zero failures and 26 stable refills (`build/live-scroll-focused.log`). A held scrollbar traversed 69,888 bytes upward and 69,892 bytes downward. The same run verifies wheel streams, PageUp/PageDown, arrow edges, repeated-reference centering, retained selection/history, the analyzed function-start wall, ambiguous prefix boundaries and refresh stability, Follow RIP release, and a real protected-page boundary.
- The full production-object `static_listing_actions_test` passed with zero failures, including live scrolling, string trace integration, and existing theme/density/zoom checks (`build/live-review-final-full-suite.log`). All 129 snapshotted production objects match the final build. The unrelated opt-in live patch-restoration case remained skipped.
- `listing_layout_test` previously passed (`build/live-assembly-scroll-core.log`), including bounded readable lookback and decoder-boundary helpers. No existing user process was attached or modified during this verification; an interactive walkthrough of the user's particular binary was not performed.
